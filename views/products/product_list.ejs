<%- include("../partials/head") %>

<body>
    <%- include("../partials/header") %>
    <main>
        <div class="container">
            <div class="container">
                <div class="row my-3">
                    <div class="col my-3">
                        <% for (let i=0; i < 1; i++) { %> <%- include
                        ("product_card", {product:products[i]}) %> <% }%>
                    </div>
                    <div class="col my-3">
                        <% for (let i=1; i < 2; i++) { %> <%- include
                        ("product_card", {product:products[i]}) %> <% }%>
                    </div>
                    <div class="col my-3">
                        <% for (let i=2; i < 3; i++) { %> <%- include
                        ("product_card", {product:products[i]}) %> <% }%>
                    </div>
                    <div class="col my-3">
                        <% for (let i=3; i < 4; i++) { %> <%- include
                        ("product_card", {product:products[i]}) %> <% }%>
                    </div>

                    <div class="col my-3">
                        <% for (let i=4; i < 5; i++) { %> <%- include
                        ("product_card", {product:products[i]}) %> <% }%>
                    </div>
                    <div class="col my-3">
                        <% for (let i=5; i < 6; i++) { %> <%- include
                        ("product_card", {product:products[i]}) %> <% }%>
                    </div>
                    <div class="col my-3">
                        <% for (let i=6; i < 7; i++) { %> <%- include
                        ("product_card", {product:products[i]}) %> <% }%>
                    </div>
                    <div class="col my-3">
                        <% for (let i=7; i < 8; i++) { %> <%- include
                        ("product_card", {product:products[i]}) %> <% }%>
                    </div>
                    <div class="col my-3">
                        <% for (let i=3; i < 4; i++) { %> <%- include
                        ("product_card", {product:products[i]}) %> <% }%>
                    </div>
                </div>
            </div>
        </div>
    </main>
    <%- include("../partials/footer") %>
</body>
